**MANDATORY: create a TodoWrite checklist with the following steps**
1. Use the test-coverage-enforcer subagent to write tests that meet or exceed our test coverage requirements.
2. Use the pre-commit-quality-guard subagent to check for compliance.

Continue this task until coverage requirements are met and all tests pass.