**MANDATORY: create a TodoWrite checklist with the following steps**
1. Use the test-coverage-enforcer subagent to check test coverage.
2. Use the test-coverage-enforcer subagent to write tests that meet or exceed our test coverage requirements. All tests must pass. There must be no errors when running tests. You MUST NOT disable tests to bypass them. You MUST NOT change tests to make them pass, unless they are incorrectly written - you must change the code to make it correct.
3. Use the pre-commit-quality-guard subagent to check for compliance.
4. Continue the previous steps until coverage meets standards and all tests pass.